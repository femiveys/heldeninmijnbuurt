export const blue = "#1890ff";
export const white = "#f0f2f5";
export const black = "#000000a6";

export const masks = (num: number) =>
  num === 1 ? "een mondmasker" : `${num} mondmaskers`;

export const theMasks = (num: number) =>
  num === 1 ? "het mondmasker" : `de ${num} mondmaskers`;
